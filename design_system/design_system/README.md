# design_system (project config package)

This directory is the Django project config package, created by `django-admin startproject design_system`. It is not a Django app and contains no views, templates, or static files. Its sole role is Design System configuration.
